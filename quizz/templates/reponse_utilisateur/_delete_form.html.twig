<form method="post" action="{{ path('reponse_utilisateur_delete', {'id': reponse_utilisateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponse_utilisateur.id) }}">
    <button class="btn">Delete</button>
</form>
