{% extends 'base.html.twig' %}

{% block title %}ReponseUtilisateur{% endblock %}

{% block body %}
    <h1>ReponseUtilisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse_utilisateur.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reponse_utilisateur_index') }}">back to list</a>

    <a href="{{ path('reponse_utilisateur_edit', {'id': reponse_utilisateur.id}) }}">edit</a>

    {{ include('reponse_utilisateur/_delete_form.html.twig') }}
{% endblock %}
