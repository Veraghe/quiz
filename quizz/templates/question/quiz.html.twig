{% extends 'base.html.twig' %}

{% block title %}Quiz{% endblock %}

{% block body %}
<div class="center container">
    <div class="ligne">
        <div class="quiz"> 
        
        {{ form_start(form2) }}
            {{ form_widget(form2) }} 

         
            <!-- Cercles qui indiquent les étapes du formulaire: -->
            <div >
                <span class="step"></span>
                <span class="step"></span>
                <span class="step"></span>
                <span class="step"></span>
                <span class="step"></span>
            </div>

            <div class="centerbtn">
                <button class="btn vert" type="button" id="nextBtn" onclick="nextPrev(1)">Suivant</button>

                <button class="btn bleu" id="validBtn">{{ button_label|default('Valider') }}</button>
                <a href="{{ path('home') }}" class="btn orange">Retour</a>
            </div>
        {{ form_end(form2) }}

        </div>
    </div>
</div>


{% endblock %}
