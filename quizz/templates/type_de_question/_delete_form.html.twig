<form method="post" action="{{ path('type_de_question_delete', {'id': type_de_question.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_de_question.id) }}">
    <button class="btn">Delete</button>
</form>
