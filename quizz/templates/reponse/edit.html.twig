{% extends 'base.html.twig' %}

{% block title %}Modifier une Réponse{% endblock %}

{% block body %}
    <div class="center container">
        <fieldset>
            <h1>Modifier une Réponse</h1>

            {{ include('reponse/_form.html.twig', {'button_label': 'Modifier'}) }}
 
            {{ include('reponse/_delete_form.html.twig') }}
            
            <a href="{{ path('question_admin') }}" class="btn orange">Annulez !</a>
        </fieldset>
    </div>
{% endblock %}
