{% extends 'base.html.twig' %}

{% block title %}Modifier un Membre{% endblock %}

{% block body %}
    <div class="center">
        <fieldset>
            <h1>Membre</h1>

            {{ include('membre/_form.html.twig', {'button_label': 'Modifier'}) }}

            {{ include('membre/_delete_form.html.twig') }}
            
            <a href="{{ path('membre_index') }}" class="btn orange">Annulez !</a>

        </fieldset>
    </div>
{% endblock %}
