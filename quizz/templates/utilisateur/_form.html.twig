{{ form_start(form) }}
    {# {{ form_widget(form) }} #}
    {{ form_row(form.nom)}}
    {{ form_row(form.prenom,{'label' : 'Prénom'})}}
    {{ form_row(form.email)}}

    {% if is_granted('ROLE_ADMIN') %}
        {{ form_row(form.roles)}}
    {% endif %}
    
    {{ form_row(form.password,{'label' : 'Mot de passe'})}}
    {{ form_row(form.confirm_password,{'label' : 'Confirmation du mot de passe'})}}
    
    <button class="btn vert">{{ button_label|default('Valider') }}</button>
    {# Pour ne pas afficher le role pendant l'inscription : #}
{# {{ form_end(form) }}  #}

